
package domain;

/**
 * This Enum serves as a status indicator of an actor.<br>
 * <br>
 *
 * Since actors represent users of diverse nature related to the domain model,
 * there is the need to state if the actor is active, banned, etc.
 */
public enum ActorStatus {

	BANNED, ACTIVE
}
